name: Deploy Hydra React

on:
  push:
    branches: ["main", "ci-test"]

  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          path: src

      - name: Checkout 2
        uses: actions/checkout@v3
        with:
          repository: crealgo/portfolio-frontend
          token: ${{ secrets.DEPLOYMENT_TOKEN }}
          path: app

      # - name: Setup Repository
      #   run: npm i

      # - name: Verify
      #   run: npm run verify

      # - name: Deploy Github Pages
      #   run: |
      #     npx gh-pages \
      #       -d test-out -u "Crealgo Bot <hello.crealgo@gmail.com>" \
      #       --repo https://git:${{ secrets.DEPLOYMENT_TOKEN }}@github.com/crealgo/crealgo-static \
      #       --dest ./test-deploy

      # - name: Deploy Fashion Greek USC Static Site
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   run: npm run site:deploy

