import styled from '@emotion/styled';


export const FileInputDisplay = styled('div')`
	display: grid;
	gap: 0.5rem;
	place-content: center;
	place-items: center;
	/* pointer-events: none; */

	.accepts {
		color: var(--color-gray-500);
	}
`;
